#include "push_swap.h"

void    ft_handle_sorting(t_list *a, t_list *b, int *sorted_ref)
{
    int i;
    a = NULL;
    b = NULL;

    i = 0;
    while (i < 5)
        printf("ref : %d\n", sorted_ref[i++]);
}